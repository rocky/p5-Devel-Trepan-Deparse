checkout:
  post:
    - sudo apt-get -y install cpanminus
    - sudo cpanm Devel::Trepan B::DeparseTree

test:
  override:
    - perl ./Build.PL
    - perl ./Build installdeps
    - GIT2CL=./git2cl make check ChangeLog distmeta distcheck
